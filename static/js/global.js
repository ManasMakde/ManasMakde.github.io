export const SITE_NAME="Manas R. Makde",SITE_DOMAIN="https://manasmakde.com",SEARCH_QUERY="search",PAGE_QUERY="page",TAGS_QUERY_PARAM="tags",BLOG_DIR="/blog/",BLOG_DATA_DIR="/static/blog-data/",BLOG_DATA_PREFIX="blog-data-",BLOG_STATS_FILE="blog-stats.json",BLOG_SEARCH_DIR="/static/blog-search/",BLOG_PAGEFIND=`${BLOG_SEARCH_DIR}pagefind.js`,ARTICLES_PER_FILE=500,DEFAULT_ARTICLES_METADATA={title:"Untitled",thumbnail:"",description:"",createdOnDate:null,editedOnDate:null,searchKeywords:[],isSearchable:!0,toPublish:!0},SNIPPET_DIR="/snippets/",SNIPPET_DATA_DIR="/static/snippets-data/",SNIPPET_DATA_PREFIX="snippet-data-",SNIPPET_STATS_FILE="snippets-stats.json",SNIPPET_SEARCH_DIR="/static/snippets-search/",SNIPPET_PAGEFIND=`${SNIPPET_SEARCH_DIR}pagefind.js`,SNIPPETS_PER_FILE=500,DEFAULT_SNIPPETS_METADATA={title:"Untitled",thumbnail:"",createdOnDate:null,editedOnDate:null,tags:[],searchKeywords:[],isSearchable:!0,toPublish:!0};export function getSearchQueryFromUrl(){return new URLSearchParams(window.location.search).get(SEARCH_QUERY)??""}export function getPageNumFromUrl(){const pageNumber=Number(new URLSearchParams(window.location.search).get(PAGE_QUERY))||1;return Math.max(pageNumber,1)}export function getTagsQueryFromUrl(){return new URLSearchParams(window.location.search).get(TAGS_QUERY_PARAM)?.split(",").filter(Boolean)??[]}export function formatDate(dateInput){if(dateInput==null)return"";let checkDateInput=new Date(dateInput);return isNaN(checkDateInput.getTime())?dateInput:new Intl.DateTimeFormat("en-GB",{day:"2-digit",month:"short",year:"numeric"}).format(checkDateInput)}export function getCleanDomain(domain){let urlString=domain.includes("://")?domain:"https://"+domain;try{return new URL(urlString).hostname.replace(/^www\./,"")}catch{return""}}export function isAbsUrl(url){try{return new URL(url),!0}catch{return!1}}export function normalizeUrl(url){return url.replace(/\\/g,"/")}export function resolveUrl(url,baseUrl,fallbackUrl=""){if(isAbsUrl(url))return url;if(!url)return fallbackUrl;const normalizedBaseUrl=normalizeUrl(baseUrl),normalizedUrl=normalizeUrl(url),cleanBaseUrl=normalizedBaseUrl.replace(/\/+$/,""),cleanUrl=normalizedUrl.replace(/^\/+/,"");return`${cleanBaseUrl}/${cleanUrl}`}
